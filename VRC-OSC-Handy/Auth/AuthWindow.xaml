<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:cefSharpCore="clr-namespace:CefSharp;assembly=CefSharp.Core"
        xmlns:cefSharpWpf="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
        xmlns:local="clr-namespace:VRC_OSC_Handy.Auth" xmlns:CefSharp="clr-namespace:CefSharp;assembly=CefSharp.Core"
    x:Class="VRC_OSC_Handy.Auth.AuthWindow"
        mc:Ignorable="d"
        Title="AuthWindow" Height="450" Width="800" ResizeMode="NoResize" AllowsTransparency="True" WindowStyle="None" Background="{x:Null}" Foreground="Lime" Loaded="Window_Loaded">

    <Window.Resources>
        <Style  x:Key="RoundedButton" TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border CornerRadius="5" x:Name="Border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#7F505050" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="10" Background="#FF282828">
        <Grid>
            <Button Content="🗙" HorizontalAlignment="Right" Margin="0,0,0,0" VerticalAlignment="Top" Width="30" Height="30" RenderTransformOrigin="0,0" Background="#00000000" Foreground="Lime" Click="Close" Style="{StaticResource RoundedButton}" BorderBrush="{x:Null}"/>
            <Button Content="🗕" HorizontalAlignment="Right" VerticalAlignment="Top" Width="30" Height="30" RenderTransformOrigin="0,0" Background="#00000000" Foreground="Lime" BorderBrush="{x:Null}" Click="Minimize" Margin="0,0,30,0" Style="{StaticResource RoundedButton}"/>
            <Rectangle Height="30" Margin="0,0,0,0" VerticalAlignment="Top" Width="740" Fill="#00000000" MouseDown="DragWindow" HorizontalAlignment="Left"/>
            <Rectangle Height="5" Margin="0,30,0,415" Fill="Lime"/>
            <cefSharpWpf:ChromiumWebBrowser x:Name="AuthBrowser" Margin="10,45,10,10"/>
        </Grid>
    </Border>
</Window>
