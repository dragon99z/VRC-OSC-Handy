<Window x:Class="VRC_OSC_Handy.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VRC_OSC_Handy"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Loaded="Window_Loaded" ResizeMode="NoResize" AllowsTransparency="True" WindowStyle="None" Background="{x:Null}" Foreground="Lime" Closing="Window_Closing">

    <Window.Resources>
        <Style  x:Key="RoundedButton" TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border CornerRadius="10" Name="Border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Button.IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#7F505050" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SliderRepeatButton" TargetType="RepeatButton">
            <Setter Property="SnapsToDevicePixels" Value="true" />
            <Setter Property="OverridesDefaultStyle" Value="true" />
            <Setter Property="IsTabStop" Value="false" />
            <Setter Property="Focusable" Value="false" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border  BorderThickness="1" BorderBrush="Black" Background="Black" Height="3"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SliderRepeatButton1" TargetType="RepeatButton">
            <Setter Property="SnapsToDevicePixels" Value="true" />
            <Setter Property="OverridesDefaultStyle" Value="true" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border SnapsToDevicePixels="True" Background="LimeGreen"  BorderThickness="1" BorderBrush="Lime" Height="3"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SliderThumb" TargetType="Thumb">
            <Setter Property="SnapsToDevicePixels" Value="true" />
            <Setter Property="OverridesDefaultStyle" Value="true" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Ellipse Height="10" Width="10" Fill="LimeGreen"></Ellipse>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="Slider"  TargetType="Slider">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Track Grid.Row="1" x:Name="PART_Track"   >
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource SliderRepeatButton1}"  Command="Slider.DecreaseLarge" />
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource SliderThumb}"  />
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource SliderRepeatButton}" Command="Slider.IncreaseLarge" />
                    </Track.IncreaseRepeatButton>
                </Track>
            </Grid>
        </ControlTemplate>

        <Style x:Key="Horizontal_Slider" TargetType="Slider">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="SnapsToDevicePixels" Value="true" />
            <Setter Property="OverridesDefaultStyle" Value="true" />
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="MinHeight" Value="25" />
                    <Setter Property="MinWidth" Value="40" />
                    <Setter Property="Template" Value="{StaticResource Slider}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="{x:Type ToggleButton}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="20" />
                </Grid.ColumnDefinitions>
                <Border
          x:Name="Border" 
          Grid.ColumnSpan="2"
          CornerRadius="0"
          Background="#FF3F3F3F"
          BorderBrush="#FF97A0A5"
          BorderThickness="1" />
                <Border 
          Grid.Column="0"
          CornerRadius="0" 
          Margin="1" 
          Background="#FF3F3F3F" 
          BorderBrush="#FF97A0A5"
          BorderThickness="0,0,1,0" />
                <Path 
          x:Name="Arrow"
          Grid.Column="1"     
          Fill="White"
          HorizontalAlignment="Center"
          VerticalAlignment="Center"
          Data="M0,0 L0,2 L4,6 L8,2 L8,0 L4,4 z"
        />
            </Grid>
            <!--<ControlTemplate.Triggers>
            <Trigger Property="ToggleButton.IsMouseOver" Value="true">
                <Setter TargetName="Border" Property="Background" Value="#808080" />
            </Trigger>
            <Trigger Property="ToggleButton.IsChecked" Value="true">
                <Setter TargetName="Border" Property="Background" Value="#E0E0E0" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Border" Property="Background" Value="#EEEEEE" />
                <Setter TargetName="Border" Property="BorderBrush" Value="#AAAAAA" />
                <Setter Property="Foreground" Value="#888888"/>
                <Setter TargetName="Arrow" Property="Fill" Value="#888888" />
            </Trigger>
        </ControlTemplate.Triggers>-->
        </ControlTemplate>

        <ControlTemplate x:Key="ComboBoxTextBox" TargetType="{x:Type TextBox}">
            <Border x:Name="PART_ContentHost" Focusable="False" Background="{TemplateBinding Background}" />
        </ControlTemplate>

        <Style x:Key="{x:Type ComboBox}" TargetType="{x:Type ComboBox}">
            <Setter Property="SnapsToDevicePixels" Value="true"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="true"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="MinHeight" Value="20"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <ToggleButton 
                    Name="ToggleButton" 
                    Template="{StaticResource ComboBoxToggleButton}" 
                    Grid.Column="2" 
                    Focusable="false"
                    IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                    ClickMode="Press">
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False"  Content="{TemplateBinding SelectionBoxItem}"
                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                    ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                    Margin="3,3,23,3"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Left" />
                            <TextBox x:Name="PART_EditableTextBox"
                    Style="{x:Null}" 
                    Template="{StaticResource ComboBoxTextBox}" 
                    HorizontalAlignment="Left" 
                    VerticalAlignment="Center" 
                    Margin="3,3,23,3"
                    Focusable="True" 
                    Background="#FF3F3F3F"
                    Foreground="Green"
                    Visibility="Hidden"
                    IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup 
                    Name="Popup"
                    Placement="Bottom"
                    IsOpen="{TemplateBinding IsDropDownOpen}"
                    AllowsTransparency="True" 
                    Focusable="False"
                    PopupAnimation="Slide">

                                <Grid Name="DropDown"
                      SnapsToDevicePixels="True"                
                      MinWidth="{TemplateBinding ActualWidth}"
                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border 
                        x:Name="DropDownBorder"
                        Background="#FF3F3F3F"

                        BorderThickness="1"
                        BorderBrush="#888888"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="#888888"/>
                            </Trigger>
                            <Trigger Property="IsGrouping" Value="true">
                                <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
                            </Trigger>
                            <Trigger SourceName="Popup" Property="Popup.AllowsTransparency" Value="true">
                                <Setter TargetName="DropDownBorder" Property="CornerRadius" Value="0"/>
                                <Setter TargetName="DropDownBorder" Property="Margin" Value="0,2,0,0"/>
                            </Trigger>
                            <Trigger Property="IsEditable"  Value="true">
                                <Setter Property="IsTabStop" Value="false"/>
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
            </Style.Triggers>
        </Style>

        <!-- SimpleStyles: ComboBoxItem -->
        <Style x:Key="{x:Type ComboBoxItem}" TargetType="{x:Type ComboBoxItem}">
            <Setter Property="SnapsToDevicePixels" Value="true"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border Name="Border"
                      Padding="2"
                      SnapsToDevicePixels="true">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="#FF4F4F4F"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="#888888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CustomCheckBox" TargetType="{x:Type CheckBox}">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#FF262E34"/>
            <Setter Property="Foreground" Value="#FF262E34"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CheckBox}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" >
                            <Border CornerRadius="3" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" Width="15" Height="15">
                                <!--                                   your color here -->
                                <Path Stretch="Uniform" Width="15" Height="10" Fill="Lime" Name="eliCheck" Data="F1 M 9.97498,1.22334L 4.6983,9.09834L 4.52164,9.09834L 0,5.19331L 1.27664,3.52165L 4.255,6.08833L 8.33331,1.52588e-005L 9.97498,1.22334 Z " Visibility="Collapsed"/>
                            </Border>
                            <TextBlock Margin="5,0,0,0"  VerticalAlignment="Center" Foreground="{TemplateBinding Foreground}" Text="{TemplateBinding Content}"></TextBlock>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="LightGray" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF9C9E9F" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="LightGray" />
                                <Setter Property="Foreground" Value="Gray" />
                                <Setter Property="BorderBrush" Value="Gray"/>
                                <Setter TargetName="eliCheck" Property="Opacity" Value="0.5" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="eliCheck" Property="Visibility" Value="Visible"></Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="10" Background="#FF282828">
        <Grid MouseMove="Grid_MouseMove">
            <Grid Name="grid_lineContainer"/>
            <Canvas Name="cvs_particleContainer"/>
            <Button Content="🗙" HorizontalAlignment="Right" Margin="0,0,0,0" VerticalAlignment="Top" Width="30" Height="30" RenderTransformOrigin="0,0" Background="#00000000" Foreground="Lime" Click="WindowClose" Style="{StaticResource RoundedButton}" BorderBrush="{x:Null}"/>
            <Button Content="🗕" HorizontalAlignment="Right" VerticalAlignment="Top" Width="30" Height="30" RenderTransformOrigin="0,0" Background="#00000000" Foreground="Lime" BorderBrush="{x:Null}" Click="Minimize" Margin="0,0,30,0" Style="{StaticResource RoundedButton}"/>
            <Rectangle Height="30" Margin="0,0,0,0" VerticalAlignment="Top" Width="740" Fill="#00000000" MouseDown="DragWindow" HorizontalAlignment="Left"/>
            <TextBlock Text="Current Avatar:" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,7.5,0,0" Foreground="Lime"/>
            <TextBlock x:Name="CurrentAvatar" Text="None" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="94,7.5,0,0" Foreground="Red"/>
            <Rectangle Height="5" Margin="0,30,0,415" Fill="Lime"/>
            <Rectangle Height="395" Fill="Lime" Width="5" Margin="0,0,0,-35"/>
            <Grid x:Name="Spotify_Controller" Margin="10,45,410,235">
                <TextBlock x:Name="Song" Uid="Spotify.Song" HorizontalAlignment="Left" Margin="10,0,0,0" TextWrapping="Wrap" Text="Current Song" VerticalAlignment="Top" FontSize="18" MouseRightButtonDown="setVRCParameterSpotify"/>
                <Button x:Name="Next" Uid="Spotify.Next" Content="⏭" HorizontalAlignment="Left" Margin="90,30,0,0" VerticalAlignment="Top" Width="25" Height="25" Background="#00000000" Foreground="Lime" Click="PlayNext" Style="{StaticResource RoundedButton}" BorderBrush="{x:Null}" FontSize="16" MouseRightButtonDown="setVRCParameterSpotify"/>
                <Button x:Name="PlayPause" Uid="Spotify.PlayPause" Content="⏯" HorizontalAlignment="Left" Margin="50,30,0,0" VerticalAlignment="Top" Width="25" Height="25" Background="#00000000" Foreground="Lime" Click="PlayPauseToggle" Style="{StaticResource RoundedButton}" BorderBrush="{x:Null}" FontSize="16" MouseRightButtonDown="setVRCParameterSpotify"/>
                <Button x:Name="Last" Uid="Spotify.Last" Content="⏮" HorizontalAlignment="Left" Margin="10,30,0,0" VerticalAlignment="Top" Width="25" Height="25" Background="#00000000" Foreground="Lime" Click="PlayLast" Style="{StaticResource RoundedButton}" BorderBrush="{x:Null}" FontSize="16" MouseRightButtonDown="setVRCParameterSpotify"/>
                <Rectangle x:Name="ProgressBar" Uid="Spotify.ProgressBar" HorizontalAlignment="Left" Height="6" Margin="10,65,0,0" VerticalAlignment="Top" Width="105" Fill="Lime" MouseRightButtonDown="setVRCParameterSpotify"/>
                <TextBlock x:Name="SotifyClientIDText" HorizontalAlignment="Left" Margin="10,85,0,0" TextWrapping="Wrap" Text="ClientID" VerticalAlignment="Top"/>
                <TextBlock x:Name="SotifyClientSecretText" HorizontalAlignment="Left" Margin="10,110,0,0" TextWrapping="Wrap" Text="ClientSecret" VerticalAlignment="Top"/>
                <PasswordBox x:Name="SotifyClientID" HorizontalAlignment="Left" Margin="150,85,0,0" VerticalAlignment="Top" Width="220" Background="{x:Null}" Foreground="Lime" Loaded="SotifyClientID_Loaded"/>
                <PasswordBox x:Name="SotifyClientSecret" HorizontalAlignment="Left" Margin="150,110,0,0" VerticalAlignment="Top" Width="220" Background="{x:Null}" Foreground="Lime" Loaded="SotifyClientSecret_Loaded"/>
                <Button x:Name="runSpotify" Content="Launch Spotify" VerticalAlignment="Top" Margin="150,135,10,0" Style="{StaticResource RoundedButton}" BorderBrush="{x:Null}" Background="#00000000" Foreground="Lime" IsEnabled="True" Click="runSpotify_Click"/>

            </Grid>
            <Rectangle Height="5" Margin="10,220,410,225" Fill="Lime"/>
            <Grid x:Name="Other_Controller" Margin="10,230,410,10">
                <TextBlock x:Name="Time" Uid="Other.Time" Text="Time" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,10,-37,0" Loaded="Time_Loaded" FontSize="18" MouseRightButtonDown="setVRCParameterOther"/>
                <Rectangle Height="5" Fill="Lime" Margin="0,40,0,166" Width="360"/>
                <TextBlock x:Name="STT" Text="STT" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,52,0,0" FontSize="18"/>

                <Button x:Name="STTEnable" Uid="Other.STT" Content="Enable STT" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,53,10,0" Background="#00000000" Foreground="Lime" Style="{StaticResource RoundedButton}" BorderBrush="{x:Null}" Click="STTEnablex_Click" MouseRightButtonDown="setVRCParameterOther" Height="20" Width="60"/>
                <TextBlock x:Name="STTRunning" Text="STT is not active" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,55,80,0" FontSize="12" Foreground="#FFFF0000" Height="20" Width="85"/>
                <ComboBox x:Name="STTModels" HorizontalAlignment="Left" VerticalAlignment="Top" FontSize="18" Width="254" Margin="115,80,0,0" Loaded="STTModels_Loaded" SelectionChanged="STTModels_SelectionChanged"/>
                <TextBlock Text="Model" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,83,0,0" FontSize="18"/>
                <ComboBox x:Name="STTLanguage" HorizontalAlignment="Left" VerticalAlignment="Top" FontSize="18" Width="254" Margin="115,120,0,0" Loaded="STTLanguage_Loaded" SelectionChanged="STTLanguage_SelectionChanged"/>
                <TextBlock Text="Language" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,123,0,0" FontSize="18"/>
                <CheckBox x:Name="STTTranslate" Content="Translate to english" Margin="10,155,0,0" Background="{x:Null}" Foreground="Lime" IsChecked="False" BorderBrush="Lime" Style="{StaticResource CustomCheckBox}" FontSize="18" HorizontalAlignment="Left" VerticalAlignment="Top" Loaded="STTTranslate_Loaded" Unchecked="STTTranslate_Unchecked" Checked="STTTranslate_Checked"/>
            </Grid>
            <Grid x:Name="VM_Controller" Margin="410,45,10,10" Loaded="VM_Controller_Loaded"/>
        </Grid>
    </Border>

</Window>
